#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int n, k = 0;
int opp[100000];
int best_point[100000];

void input(){
    cin >> n >> k;
    for (int i = 0; i < n; i++)
    {
        int x;
        cin >> x;
        opp[i] = x;
    }
}

int main(){
    input();
    for (int i = 0; i < n; i++)
    {
        /* code */
    }
    
}
